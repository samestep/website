import { Content } from "../../../blog";

export const content: Content = async () => {
  return {
    histogramNaive: <p>(TODO: insert histogram here)</p>,
    histogramEight: <p>(TODO: insert another histogram here)</p>,
    histogramDie: <p>(TODO: finally, a uniform die roll histogram)</p>,
  };
};
