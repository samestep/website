{
  "private": true,
  "devDependencies": {
    "@types/bun": "^1",
    "chokidar": "^3",
    "live-server": "^1",
    "prettier": "^3",
    "prettier-plugin-organize-imports": "^4"
  },
  "scripts": {
    "build": "./build.ts",
    "serve": "live-server dist --verbose"
  }
}
